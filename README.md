# IL (ice logger)

Is a CLI Logger thing to save `.log` files fast directly from the Terminal.

## How does it work?

Use `il "{log_text} {log_name}"` to create a log.

## Requirements

macOS 12 or higher

## How many updates will it get?

Uhh... Idk
